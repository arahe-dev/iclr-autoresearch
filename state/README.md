# Champion state

`champion.json` is the tracked promotion pointer. A candidate may start from the champion branch, but a new champion branch is created only after the recorded correctness, B32, B64, target, and memory gates pass. Reverted candidates remain in history and are never deleted.

